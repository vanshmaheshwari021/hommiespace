{
  "version": 2,
  "name": "hommiespace-admin-portal",
  "buildCommand": "cd ../.. && npm run build --workspace=packages/shared && npm run build --workspace=apps/admin",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
